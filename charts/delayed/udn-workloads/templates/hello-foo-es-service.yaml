apiVersion: v1
kind: Service
metadata:
  name: hello-foo
  namespace: udn-foo
  annotations:
    metallb.io/address-pool: floatingips
spec:
  selector:
    app: hello-foo-es
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
---